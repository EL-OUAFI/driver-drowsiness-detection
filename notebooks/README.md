# Notebooks

This directory stores the original exploratory notebooks that informed the refactor.

They are intentionally kept separate from the main package so the repository has:

- a clean implementation path in `src/`
- preserved research artifacts for traceability

If you are reviewing the project for engineering quality, start with the package and CLI rather than the notebooks.
